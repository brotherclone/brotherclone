## 1. Type
- [x] 1.1 Add `hasCommission?: boolean` to `DropProduct` in `lib/fourthwall.ts`

## 2. UI
- [x] 2.1 Add disclosure label to `app/page.tsx` — render when `mode === "affiliate" && hasCommission === true`
- [x] 2.2 Add `.drop-card__disclosure` style to `app/page.module.scss`

## 3. Docs
- [x] 3.1 Update `docs/drop-modes.md` affiliate section to document `hasCommission` and when to set it
